services:
  description-extractor:
    build: .
    image: streamlit-description-extractor:local
    ports:
      - "8502:8501"
    environment:
      STREAMLIT_SERVER_MAX_UPLOAD_SIZE: "500"
    restart: unless-stopped
